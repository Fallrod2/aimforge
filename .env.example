# Clé API Anthropic — utilisée UNIQUEMENT côté serveur (src/server/**).
# Ne jamais l'importer depuis src/client/** : elle finirait dans le bundle.
# Copier ce fichier en `.env` et remplacer la valeur.
ANTHROPIC_API_KEY=sk-ant-...

# Port de l'API Hono (optionnel, défaut 3000).
PORT=3000
